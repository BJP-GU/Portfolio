## Running the Local Server on Port 8000

To serve the portfolio locally on `http://localhost:8000`:

```bash
cd /Users/bryceparsons/Desktop/Portfolio
python3 -m http.server 8000
```

- **Stop the server**: Press `Ctrl + C` in the terminal where the command is running.

